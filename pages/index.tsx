import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <main
      className="d-flex align-items-center justify-content-center"
      style={{ height: '100vh' }}
    >
      Hi!
    </main>
  );
};

export default Home;
